/**
 * Scan Tab - Re-exports existing ScanScreen component
 * Uses the existing camera/gallery + API prediction logic
 */
import { ScanScreen } from '../../src/screens/ScanScreen';
export default ScanScreen;
